{
  "name": "OrgSE",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": [
    "module",
    "CommonJS"
  ],
  "scripts": {
    "dev": "nodemon --watch './' --exec 'node index.js' -e js",
    "backend": "json-server --watch ./assets/backend.json --port 5000",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "start-dev": "nodemon index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2",
    "express": "^4.18.2",
    "express-flash": "^0.0.2",
    "express-handlebars": "^6.0.6"
  },
  "devDependencies": {
    "json-server": "^0.17.1",
    "nodemon": "^2.0.20"
  }
}
